<%= render '/shared/page_header', header: "Edit your question", subtext:"Note that the answer right now is: #{@question.answer.body}" %>

<%= render 'form', question: [@exam, @question] %>

<%= link_to 'Cancel Edit', new_exam_question_path(@exam) %>
